import CanvasST from "@/app/[locale]/_components/canvases/CanvasST";
import TLToolbar from "@/app/[locale]/_components/canvases/custom-ui/TLToolbar/TLToolbar";



// TODO: use intercepting routes or something to handle if the connected user is the owner of the room

export default async function RoomPage() {
    /*
    return (
        <DeskLayout
            toolBar={<TLToolbar />}
            canvas={<CanvasST />}
        />
    )*/

    return (
        <div>Student view</div>
    )
}